{% extends 'base2.html.twig' %}

{% block title %}AdminRooms{% endblock %}

{% block body %}
    <h1>AdminRooms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_room.id }}</td>
            </tr>
            <tr>
                <th>Room_Name</th>
                <td>{{ admin_room.RoomName }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ admin_room.Capacity }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ admin_room.Status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_rooms_index') }}">back to list</a>

    <a href="{{ path('app_admin_rooms_edit', {'id': admin_room.id}) }}">edit</a>

    {{ include('admin_rooms/_delete_form.html.twig') }}
{% endblock %}
